# ====================
# Environment Variables Template
# ====================
# This is a general template for all environments
# For specific environments, use:
# - .env.local.example (Local development)
# - .env.ci (GitHub CI)
# - .env.production.example (Production)

# To set up your environment:
# 1. Copy the appropriate example file:
#    - Local: cp .env.local.example .env.local
#    - Prod: cp .env.production.example .env.production
# 2. Fill in your actual values
# 3. NEVER commit .env.local or .env.production to git

# ====================
# Database
# ====================
# For local development:
DATABASE_URL="postgresql://user:password@localhost:5432/database?schema=public"
# For GitHub Codespaces:
# DATABASE_URL="postgresql://dev_user:dev_password@db:5432/timetable_dev?schema=public"


# ====================
# Better Auth
# ====================
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-here"
BETTER_AUTH_URL="http://localhost:3000"

# ====================
# Google OAuth (Better Auth)
# ====================
# Get from: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# ====================
# Email (SMTP / Nodemailer)
# ====================
# Used for better-auth email verification (e.g. change email flow).
# Set these in production hosting env vars; for local dev you can point to a local SMTP server.
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_SECURE="false" # true for 465, false for 587/25
# SMTP_USER="smtp-username"
# SMTP_PASSWORD="smtp-password"
# EMAIL_FROM="Phrasongsa Timetable <no-reply@example.com>"


# ====================
# App Configuration
# ====================
# Base URL for the application (used by better-auth client)
NEXT_PUBLIC_APP_URL="http://localhost:3000"


# ====================
# API Configuration
# ====================
NEXT_PUBLIC_API_HOST="http://localhost:3000"

# ====================
# Admin Credentials (Security - Issue #155)
# ====================
# Default admin password for seeding and E2E tests
# - Local/CI: Leave empty to use default "admin123"
# - Production: MUST set a strong password
# SEED_ADMIN_PASSWORD="" # Used during pnpm db:seed
# ADMIN_PASSWORD=""       # Used by E2E tests

# ====================
# Environment Selection Guide
# ====================
# Local Development:
#   1. Copy .env.local.example to .env.local
#   2. Start with: pnpm dev
#
# GitHub CI:
#   Automatically uses .env.ci
#   Configured in .github/workflows/*.yml
#
# Production:
#   1. Copy .env.production.example to .env.production
#   2. Set all variables in your hosting platform
#   3. Never commit production secrets
