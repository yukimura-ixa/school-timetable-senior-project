# Database Configuration
# MySQL connection string for local development
DATABASE_URL="mysql://root:password@localhost:3306/school-timetable-db-dev?connection_limit=40&connect_timeout=0&pool_timeout=0"

# API Configuration
# Backend API base URL - used by axios for all API calls
# In development, this should point to your local API server
# In production, set this to your production API URL
NEXT_PUBLIC_API_HOST="http://localhost:3001/api"

# NextAuth Configuration
# Base URL for the application
NEXTAUTH_URL="http://localhost:3000"
# Secret key for NextAuth.js (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-key-here"

# Google OAuth Configuration (for production authentication)
# Get these from Google Cloud Console: https://console.cloud.google.com/
NEXT_GOOGLE_AUTH_CLIENT_ID="your-google-client-id"
NEXT_GOOGLE_AUTH_CLIENT_SECRET="your-google-client-secret"

# ⚠️ SECURITY: Development/Testing Bypass (OPT-IN ONLY)
# This is a SERVER-ONLY variable and will NOT be embedded in the client bundle
# 
# Set to "true" ONLY in local development to enable dev bypass login
# MUST be "false" or unset in production - NEVER deploy with this enabled
# 
# When enabled:
# - A "Dev Bypass" button appears on login pages
# - Users can log in without Google OAuth
# - Authentication checks are bypassed in middleware
#
# Defaults to disabled (false) for security
ENABLE_DEV_BYPASS="false"

# Development User Configuration (only used when ENABLE_DEV_BYPASS="true")
# These settings define the test user created by the dev bypass
DEV_USER_EMAIL="admin@test.com"
DEV_USER_ROLE="admin"
DEV_USER_ID="1"
DEV_USER_NAME="Test Admin"

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values (especially NEXTAUTH_SECRET and Google OAuth credentials)
# 3. For local testing with bypass: Set ENABLE_DEV_BYPASS="true" in your .env
# 4. NEVER commit .env to version control
# 5. ALWAYS verify ENABLE_DEV_BYPASS is disabled before deploying to production
