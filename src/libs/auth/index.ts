/**
 * Auth.js v5 (NextAuth beta) Configuration
 * 
 * This module exports authentication utilities for the application.
 * Uses Google OAuth and optional development bypass for testing.
 * 
 * @see https://authjs.dev/getting-started/installation
 */

export { auth, handlers, signIn, signOut, authConfig } from "./config"
