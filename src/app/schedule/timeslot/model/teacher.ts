type teacher = { //ของจริงน่าจะต้อง join ตารางมาลงอาจจะมีเยอะกว่านี้ ละก็จะเปลี่ยนชื่อ type เพราะใช้ซ้ำไม่ได้
    TeacherID : number;
    FirstName : string;
    LastName : string;
    Department : string;
}