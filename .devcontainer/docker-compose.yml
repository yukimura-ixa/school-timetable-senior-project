services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/school-timetable-senior-project:cached
    network_mode: service:db
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      - POSTGRES_USER=dev_user
      - POSTGRES_PASSWORD=dev_password
      - POSTGRES_DB=timetable_dev
    volumes:
      # postgres:18+ refuses a mount at /var/lib/postgresql/data; it stores data
      # under /var/lib/postgresql/<major>/ so the parent must be the mount.
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dev_user -d timetable_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
