# Production E2E Testing Environment (Slow - Uses Vercel Postgres via Prisma Accelerate)
# This configuration uses the production database for smoke tests and production verification
# USE THIS SPARINGLY - only for critical smoke tests
# 
# IMPORTANT: This file contains PLACEHOLDERS only. 
# Actual secrets are injected via GitHub Secrets in CI or from .env.local for local testing.

# Vercel Production Database via Prisma Accelerate (Slow but production-like)
# CI: Injected from secrets.DATABASE_URL
# Local: Copy from .env and uncomment below
# DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY_HERE"

# E2E Test Database (docker-compose.test.yml)
# Uses PostgreSQL container on localhost:5433
DATABASE_URL="postgresql://test_user:test_password@localhost:5433/test_timetable?schema=public&connection_limit=10"

# Direct Postgres URL (optional - for migrations)
# CI: Injected from secrets.POSTGRES_URL
# Local: Copy from .env and uncomment below
# POSTGRES_URL="postgres://USER:PASSWORD@db.prisma.io:5432/postgres?sslmode=require"

# Auth Configuration (Better Auth)
BETTER_AUTH_URL=http://localhost:3005
# CI: Injected from secrets.BETTER_AUTH_SECRET
BETTER_AUTH_SECRET="test-secret-at-least-32-characters-long-for-testing"
# Legacy fallback (Better Auth checks both)
AUTH_SECRET="test-secret-at-least-32-characters-long-for-testing"
# CI: Injected from secrets.AUTH_GOOGLE_ID and secrets.AUTH_GOOGLE_SECRET
AUTH_GOOGLE_ID="your-google-client-id-here"
AUTH_GOOGLE_SECRET="your-google-client-secret-here"

# Seed API Secret (for seed endpoint tests)
# CI: Injected from secrets.SEED_SECRET
# SEED_SECRET="your-seed-secret-here"

# Test Configuration - Production mode for accurate testing
NODE_ENV=test

# Playwright base URL (aligns with test webserver port)
BASE_URL=http://localhost:3005

# Admin credentials for E2E/local testing
ADMIN_EMAIL=admin@school.local
ADMIN_PASSWORD=admin123
