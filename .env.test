# E2E test environment overrides
# Test environment - uses local PostgreSQL test database
# This file is loaded during E2E tests via dotenv-cli

# Auth secret (dummy for tests)
AUTH_SECRET=testing-secret-not-for-prod

# Seed API secret (for seed endpoint E2E tests)
# Must match the SEED_SECRET used by dev server during E2E tests
SEED_SECRET="df83c9b4a1e2f5d6c3a8b9e0f1d2c3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0"

# Prisma Studio Local Proxy (Using Prisma Accelerate-style connection)
# This connects through Prisma's local proxy server which manages connection pooling
# Actual database: postgres://postgres:postgres@localhost:51214/template1
# Direct connection to local Docker test database (port 5433)
DATABASE_URL="postgresql://test_user:test_password@127.0.0.1:5433/test_timetable?schema=public"
POSTGRES_URL="postgresql://test_user:test_password@127.0.0.1:5433/test_timetable?schema=public"

# Old Prisma Accelerate Proxy Config (Commented out)
# DATABASE_URL="prisma+postgres://localhost:51213/?api_key=..."
# POSTGRES_URL="prisma+postgres://localhost:51213/?api_key=..."
